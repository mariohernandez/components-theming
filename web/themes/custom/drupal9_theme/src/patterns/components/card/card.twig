{{ attach_library('drupal9_theme/card') }}

<article class="card{{- modifier ? ' ' ~ modifier -}}
  {{- attributes ? ' ' ~ attributes.class -}}"
  {{- attributes ? ' ' ~ attributes|without(class) -}}>
  {% if image %}
    <div class="card__image">
      {{ image }}
    </div>
  {% endif %}

  <div class="card__content">
    {% if date %}
      {%
        include '@drupal9_theme/eyebrow/eyebrow.twig' with {
          "modifier": 'card__date',
          "text": date,
        } only
      %}
    {% endif %}

    {% if heading %}
      {{ title_prefix }}
      {% include '@drupal9_theme/heading/heading.twig' %}
      {{ title_suffix }}
    {% endif %}

    {% if body %}
      <p class="card__body">
        {{ body }}
      </p>
    {% endif %}

    {% if cta %}
      {% include '@drupal9_theme/button/button.twig' with {
          "button": cta,
        } only
      %}
    {% endif %}
  </div>
</article>
